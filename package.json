{
  "name": "travis-env",
  "version": "0.0.3",
  "description": "A tool for provisioning secrets into travis build environments",
  "repository": {
    "type": "git",
    "url": "https://github.com/eventology/travis-env.git"
  },
  "scripts": {
    "lint": "eslint *.js",
    "build": "tsc --build tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "travis-env": "build/cmd.js"
  },
  "eslintConfig": {
    "extends": [
      "@eventology/eslint-config"
    ]
  },
  "keywords": [
    "travis",
    "ci",
    "github",
    "auto",
    "environment",
    "shell",
    "helper"
  ],
  "author": "Chance Hudson",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.294.0",
    "bluebird": "^3.5.1",
    "yargs": "^12.0.1"
  },
  "devDependencies": {
    "@eventology/eslint-config": "0.0.16",
    "@types/aws-sdk": "^2.7.0",
    "@types/bluebird": "^3.5.23",
    "@types/node": "^10.7.1",
    "@types/yargs": "^11.1.1",
    "eslint": "^5.3.0",
    "typescript": "^3.0.1"
  }
}
